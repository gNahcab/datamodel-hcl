use arrow::*;

fn main() {
    let primitive_array = array();
    println!("{:?}", primitive_array);
}
//todo: array would be part of ports-directory
fn into_array() -> StringArray {
    let mut string_array_builder = StringBuilder::new(row_number);
}
#[cfg(test)]
mod test {

    #[test]
    fn test_sth() {
        xlsx_path = "data/testdata/OldExcelDocument.xlsx";
        assert!(Ok(()));
    }
}



